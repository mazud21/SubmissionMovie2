<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.hmazud.submissionmovie2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <permission
        android:name="com.gmail.hmazud.submissionmovie2.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="com.gmail.hmazud.submissionmovie2.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailMovie" />
        <activity android:name=".SearchMovie" />

        <provider
            android:name=".provider.FavoriteProvider"
            android:authorities="com.gmail.hmazud.submissionmovie2"
            android:exported="true"
            android:readPermission="com.gmail.hmazud.submissionmovie2.READ_DATABASE"
            android:writePermission="com.gmail.hmazud.submissionmovie2.WRITE_DATABASE" />

        <receiver android:name=".widget.FavWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/fav_widget_info" />
        </receiver>
        <receiver
            android:name=".service.AlarmApps"
            android:enabled="true"
            android:exported="false" />

        <service
            android:name=".service.SchedulerService"
            android:enabled="true"
            android:exported="false"
            android:permission="com.google.android.gms.permission.BIND_NETWORK_TASK_SERVICE">
            <intent-filter>
                <action android:name="com.google.android.gms.gcm.ACTION_TASK_READY" />
            </intent-filter>
        </service>
        <service
            android:name=".widget.WidgetService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <activity
            android:name=".settings.SettingsService"
            android:label="@string/title_activity_settings_service"/>
    </application>

</manifest>